# Node Environment
NODE_ENV=development
PORT=5001
CLIENT_URL=http://localhost:5173

# Database Configuration
DB_NAME=phantomhub
DB_USER=joshuafisher
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_SSL=false
DB_SSL_CA=
DB_POOL_MAX=10
DB_POOL_MIN=2
DB_POOL_ACQUIRE=30000
DB_POOL_IDLE=10000
DB_RETRY_ATTEMPTS=5
DB_RETRY_DELAY=5000

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_PREFIX=phantomhub

# JWT Configuration
JWT_SECRET=phantom_hub_super_secret_key
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# Rate Limiting
RATE_LIMIT_WINDOW=900000  # 15 minutes in milliseconds
RATE_LIMIT_MAX=100        # Maximum requests per window
RATE_LIMIT_DELAY=0        # Delay between requests (0 for no delay)

# Security
CORS_ORIGIN=http://localhost:5173
SECURE_COOKIE=false       # Set to true in production
SESSION_SECRET=phantom_hub_session_secret

# Logging
LOG_LEVEL=debug
LOG_FORMAT=dev           # 'dev' or 'combined'
LOG_FILE=logs/app.log   # Log to file in production

# Logging
LOG_LEVEL=debug 